# Continue Here — 2026-04-05

## 현재 상태
NICU 전용 버전 (parents-education) — 관리자 에디터 개선 완료, 안정화 단계

## 폴더 구조
```
바이브코딩/
├── parents-education/   ← NICU 전용 (이 폴더, 현재 작업 중)
└── severance_app/       ← 전체 부서 버전 (복사본, 독립 관리)
```

## 완료된 작업

### v2 마일스톤 (다중 부서 확장 → NICU로 분리)
- Phase 1-8: 전체 구현 후 severance_app으로 복사
- parents-education은 NICU 전용으로 되돌림 (관리자 개선은 유지)

### 관리자 에디터 개선 (양쪽 버전 공통)
- ✅ 실시간 미리보기 패널 (좌: 편집, 우: 보호자 화면)
- ✅ 이미지 업로드 (파일 + URL, 위치/크기/캡션)
- ✅ 굵은 글씨 (**bold**) 토글 지원
- ✅ 태그/그룹 드롭다운 (추가/수정/삭제)
- ✅ Q&A → AI 챗봇 대신 관리자 편집 카드 방식
- ✅ 알림 예약 발송 (즉시/예약, 날짜+시간)
- ✅ 보호자 알림 팝업 (확인/닫기 버튼)
- ✅ 관리자 로그인 이메일+비밀번호 방식

## 로그인 정보

### NICU (parents-education)
- 관리자: http://localhost:3000/login?role=staff
  - admin-nicu@severance.com / password123
- 보호자: http://localhost:3000 → 등록번호 0561528

### 전체 부서 (severance_app)
- 관리자: admin-nicu@, admin-ortho@, admin-ccu@, admin-er@ (모두 @severance.com / password123)
- 보호자: 동일

## Vite 프록시 설정
- `vite.config.ts` → proxy target: `http://localhost:3001` (로컬 개발)
- Vercel 배포 시 프록시 불필요 (서버리스 함수가 처리)

## 알려진 이슈
- sql.js 인메모리 DB → 서버 재시작 시 데이터 초기화 (Vercel cold start 포함)
- 이미지 업로드 파일은 public/uploads/에 저장 → Vercel 배포 시 영속 안 됨
- 기존 NICU 하드코딩 콘텐츠(admissionContent, treatmentContent 등)는 ParentDashboard에 남아있음

## 미완료 / 다음 작업 후보
- [ ] Vercel 배포 테스트 (parents-education + severance_app 각각)
- [ ] 이미지 영속 저장소 (Cloudinary, S3 등 외부 스토리지)
- [ ] DB 영속화 (Turso, PlanetScale 등)
- [ ] 비밀번호 변경 기능 (현재 하드코딩된 password123)
- [ ] 보호자 비밀번호 보안 강화 (현재 등록번호만으로 로그인)
- [ ] severance_app 독립 Git 저장소 초기화

## 재개 방법
```bash
cd /Users/dimi/Desktop/바이브코딩/parents-education
npm run dev
# 서버: localhost:3001, 프론트: localhost:3000
```
